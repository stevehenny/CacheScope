                 ┌───────────────────┐
                 │   User Input      │
                 │  (binary, options)│
                 └─────────┬─────────┘
                           │
             ┌─────────────┴─────────────┐
             │                           │
      ┌───────────────┐           ┌───────────────┐
      │Static Analyzer│           │Dynamic Analyzer│
      │(DWARF / ELF)  │           │(Memory Tracer) │
      └───────────────┘           └───────────────┘
             │                           │
             │                           │
             │                           │
  ┌──────────┴───────────┐   ┌───────────┴───────────┐
  │  StructRegistry      │   │ Runtime Memory Trace  │
  │  - Structs/Classes   │   │  - Address / Thread / │
  │  - Field offsets     │   │    Instruction info   │
  │  - Field sizes       │   │  - Load / Store ops   │
  │  - Padding info      │   │  - context switching/
  │                      |    |    migrations
  └──────────┬───────────┘   └───────────┬───────────┘
             │                           │
             └─────────────┬─────────────┘
                           │
                  ┌────────┴────────┐
                  │  Analyzer Core  │
                  │ - Map memory    │
                  │    addresses →  │
                  │    struct fields│
                  │ - Detect        │
                  │    false sharing│
                  │ - Hot/cold field│
                  │ - Padding ineff │
                  └────────┬────────┘
                           │
                   ┌───────┴─────────┐
                   │  Report / Output│
                   │  - Text or JSON │
                   │  - Heatmaps     │
                   │  - Cache Line   │
                   │   visualizations│
                   └─────────────────┘



Detecting false sharing:

1. Look for hot cache lines shared among tids
2. If a lot of tids are writing to the same cache line, and are running on separate cpus, raise red flag
3. No kernel memory cache lines are candidates for false_sharing (user has little control over that)
